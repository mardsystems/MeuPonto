@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo($"app/authentication/login?returnUrl={Uri.EscapeDataString(Navigation.Uri)}");
    }
}
